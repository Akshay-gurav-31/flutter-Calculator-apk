# Calculator App

A premium Flutter calculator app.

## How to run
1. Ensure Flutter is installed and added to your PATH.
2. Run `flutter run` in this directory.

## How to build APK
1. Run `flutter build apk --release`.
